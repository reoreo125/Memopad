<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="Reoreo125.Memopad.Views.Windows.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Reoreo125.Memopad.ViewModels.Windows"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="バージョン情報" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <StackPanel Margin="0,0,0,10">
            <TextBlock Text="Memopad" FontSize="24" FontWeight="Bold" TextAlignment="Center" />
            <TextBlock Text="Version 1.0.0" FontSize="14" TextAlignment="Center" Margin="0,10,0,0" />
            <TextBlock Text="A simple text editor built with WPF and R3" FontSize="12" TextAlignment="Center" Foreground="Gray" Margin="0,10,0,0" />
        </StackPanel>
        <Button Content="OK" 
                Width="100" 
                Height="40"
                Command="{Binding CloseAboutCommand}"
                HorizontalAlignment="Center" 
                Margin="0,20,0,0" />
    </StackPanel>
</Window>
