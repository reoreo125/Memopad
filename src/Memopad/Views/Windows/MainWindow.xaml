<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="Reoreo125.Memopad.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="{Binding Title.Value}" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">
    <DockPanel>
        <!-- Menu Component -->
        <views:MemopadMenuView DockPanel.Dock="Top" 
                              xmlns:views="clr-namespace:Reoreo125.Memopad.Views.Components"
                              prism:ViewModelLocator.AutoWireViewModel="True" />

        <!-- Status Bar Component -->
        <views:MemopadStatusBarView DockPanel.Dock="Bottom" 
                                   xmlns:views="clr-namespace:Reoreo125.Memopad.Views.Components"
                                   prism:ViewModelLocator.AutoWireViewModel="True" />

        <!-- Content Area -->
        <Grid>
            <TextBox Name="MemoTextBox" 
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     FontFamily="Consolas"
                     FontSize="12"
                     Padding="5"
                     TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Visible"
                     HorizontalScrollBarVisibility="Visible"
                     Text="{Binding Text.Value, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>
    </DockPanel>
</Window>

