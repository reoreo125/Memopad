<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Reoreo125.Memopad.Views.Components.MemopadMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <Menu>
        <MenuItem Header="ファイル(_F)">
            <MenuItem Header="新規作成(_N)" InputGestureText="Ctrl+N" />
            <MenuItem Header="開く(_O)" InputGestureText="Ctrl+O" Command="{Binding OpenTextFileWindowCommand}" />
            <MenuItem Header="保存(_S)" InputGestureText="Ctrl+S" />
            <MenuItem Header="名前を付けて保存(_A)" InputGestureText="Ctrl+Shift+S" />
            <Separator />
            <MenuItem Header="終了(_X)" InputGestureText="Alt+F4" Command="{Binding ApplicationExitCommand}" />
        </MenuItem>
        <MenuItem Header="編集(_E)">
            <MenuItem Header="元に戻す(_U)" InputGestureText="Ctrl+Z" />
            <MenuItem Header="やり直す(_R)" InputGestureText="Ctrl+Y" />
            <Separator />
            <MenuItem Header="切り取り(_T)" InputGestureText="Ctrl+X" />
            <MenuItem Header="コピー(_C)" InputGestureText="Ctrl+C" />
            <MenuItem Header="貼り付け(_P)" InputGestureText="Ctrl+V" />
            <Separator />
            <MenuItem Header="すべて選択(_A)" InputGestureText="Ctrl+A" />
        </MenuItem>
        <MenuItem Header="表示(_V)">
            <MenuItem Header="折り返し(_I)" IsCheckable="True" IsChecked="True" />
        </MenuItem>
        <MenuItem Header="ヘルプ(_H)">
            <MenuItem Header="バージョン情報(_V)" Command="{Binding OpenAboutWindowCommand}" />
        </MenuItem>
    </Menu>
</UserControl>

