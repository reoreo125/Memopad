<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Reoreo125.Memopad.Views.Components.MemopadStatusBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Reoreo125.Memopad.ViewModels.Components"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <StatusBar Height="24">
        <StatusBarItem HorizontalAlignment="Right">
            <StackPanel Orientation="Horizontal">
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}"/>
                <TextBlock Text="{Binding PositionText.Value, UpdateSourceTrigger=PropertyChanged}" Margin="5 0 5 0" Width="110" Foreground="Black" HorizontalAlignment="Left"/>
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}"/>
                <TextBlock Text="100%" Margin="5 0 5 0" Width="40" Foreground="Black" HorizontalAlignment="Left"/>
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}"/>
                <TextBlock Text="{Binding LineEndingText.Value, UpdateSourceTrigger=PropertyChanged}" Margin="5 0 5 0" Width="110" Foreground="Black" HorizontalAlignment="Left"/>
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}"/>
                <TextBlock Text="{Binding EncodingText.Value, UpdateSourceTrigger=PropertyChanged}" Margin="5 0 5 0" Width="100" Foreground="Black" HorizontalAlignment="Left"/>
            </StackPanel>
        </StatusBarItem>
    </StatusBar>
</UserControl>
