<UserControl x:Class="Reoreo125.Memopad.Views.Dialogs.ConfirmSaveDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Reoreo125.Memopad.Views.Dialogs"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="ResizeMode" Value="NoResize"/>
            <Setter Property="ShowInTaskbar" Value="False"/>
            <Setter Property="SizeToContent" Value="WidthAndHeight"/>
        </Style>
    </prism:Dialog.WindowStyle>
    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="保存する(_S)" Command="{Binding SaveCommand}" Width="85" Margin="5"/>
                <Button Content="保存しない(_N)" Command="{Binding DontSaveCommand}" Width="85" Margin="5"/>
                <Button Content="キャンセル" Command="{Binding CancelCommand}" Width="85" Margin="5"/>
            </StackPanel>
        </StatusBar>
        <Grid Margin="10">
            <TextBlock Text="{Binding Message}" Margin="0,0,0,20" FontSize="14"/>
        </Grid>
    </DockPanel>

</UserControl>
