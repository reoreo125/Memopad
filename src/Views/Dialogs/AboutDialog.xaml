<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Reoreo125.Memopad.Views.Dialogs.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Reoreo125.Memopad.ViewModels.Windows"
        xmlns:prism="http://prismlibrary.com/"
        mc:Ignorable="d"
        Height="200" Width="400">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="ResizeMode" Value="NoResize"/>
            <Setter Property="ShowInTaskbar" Value="False"/>
            <Setter Property="SizeToContent" Value="WidthAndHeight"/>
        </Style>
    </prism:Dialog.WindowStyle>
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <StackPanel Margin="0,0,0,10">
            <TextBlock Text="{Binding Message}" FontSize="24" FontWeight="Bold" TextAlignment="Center" />
            <Separator />
            <TextBlock Text="{Binding VersionText}" FontSize="14" TextAlignment="Center" Margin="0,10,0,0" />
            <TextBlock Text="A simple text editor built with WPF and Prism, R3" FontSize="12" TextAlignment="Center" Foreground="Gray" Margin="0,10,0,0" />
        </StackPanel>
        <Button Content="OK" 
                Width="100" 
                Height="26"
                Command="{Binding OkCommand}"
                HorizontalAlignment="Center" 
                Margin="0,20,0,0" />
    </StackPanel>
</UserControl>
